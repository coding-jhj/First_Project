# 2026-04-30 날짜별 정리

## 오늘 목표

- GCP Cloud Run 서버 배포를 완료한다.
- Android 앱이 서버와 온디바이스 탐지를 모두 사용할 수 있게 정리한다.
- 서버 URL 입력, fallback, bbox 표시, 권한 분리를 안정화한다.

## 사용모델

- YOLO 객체 탐지 모델
- ONNX Runtime Android
- Depth V2 모델 또는 bbox fallback
- FastAPI
- GCP Cloud Run
- Android TTS/STT

## 구동원리

- GCP Cloud Run에 FastAPI 서버를 배포하고 `/health`, `/detect`, `/dashboard` 엔드포인트를 제공한다.
- Android는 서버 URL이 있으면 서버 기능을 사용할 수 있고, 서버 실패 시 온디바이스 ONNX 탐지로 fallback한다.
- 장애물/찾기 모드는 서버 상태에 묶이지 않도록 온디바이스 우선으로 동작한다.
- 서버 전송 이미지는 EXIF 회전을 보정한 뒤 업로드해 세로 사진이 잘못 회전되는 문제를 막는다.
- bbox 위험도 색상은 빨강, 노랑, 초록으로 구분한다.

## 목표 실제 구동 유,무

- 구동 확인.
- 서버 URL: `https://voiceguide-1063164560758.asia-northeast3.run.app`
- `/health` 정상 응답 확인.
- GCP 배포, Android fallback, EXIF 회전 보정, 권한 분리, bbox 색상 구분이 반영됨.
- APK 재빌드와 실제 폰 설치 테스트는 남은 작업으로 기록됨.

## 내일 목표

- 실제 기기에서 APK를 설치하고 장애물 인식, TTS 음성, 서버 fallback을 확인한다.
- 발표 준비를 위해 동작 확인 항목과 한계 표현을 정리한다.
- 5월 13일 최종 발표 기준으로 MVP 설명을 다듬는다.

