# 2026-05-04 변경 이력

> 범례: ✅ 반영 완료 | ❌ 반영 실패/롤백 | ⚠️ 부분반영/미검증 | 🗑️ 제거

## 핵심 이벤트

Android FPS 8~9 → 10+ 구조 완성. 메모리 직접 캡처로 전환, TTS-텍스트 동기화 해결.  
물건확인 모드 추가. **OBB 라우팅 코드 추가됐지만 모델 미검증 상태.**  
계단 탐지 온디바이스 한정으로 재통합 (feature/jaehyun).

---

## 수정 전 / 수정 후 — Android 성능

| 항목 | 수정 전 | 수정 후 | 상태 |
|------|---------|---------|------|
| INTERVAL_MS | 100ms | 50ms (스케줄러 처리 완료 직후 즉시 시작) | ✅ |
| ONNX 스레드 수 | 기본값 1개 | setIntraOpNumThreads(2) (CPU 코어 2개 활용) | ✅ |
| 캡처 방식 | OnImageSavedCallback (File I/O) — JPEG 저장→읽기 왕복 | OnImageCapturedCallback (메모리 직접 처리, 프레임당 ~20ms 절약) | ✅ |

---

## 수정 전 / 수정 후 — TTS·UX

| 항목 | 수정 전 | 수정 후 | 상태 |
|------|---------|---------|------|
| TTS-텍스트 동기화 | tvStatus 즉시 갱신, 음성 수십~수백ms 후 출력 | pendingStatusText → onStart() 콜백에서 업데이트 | ✅ |
| SentenceBuilder 쉼표 | "오른쪽 약 2미터에 의자가 있어요." | "오른쪽 약 2미터에, 의자가 있어요." (TTS 자연스러운 쉼 유도) | ✅ |
| accessibilityLiveRegion | polite (다른 알림 끝난 뒤 읽음) | assertive (TalkBack 즉시 읽음) | ✅ |

---

## 수정 전 / 수정 후 — 기능

| 항목 | 수정 전 | 수정 후 | 상태 |
|------|---------|---------|------|
| 물건확인 모드 | 없음 | 추가 (카메라 앞 물체 서술형 안내) | ✅ |
| 계단 탐지 (온디바이스) | 4/28에 전면 제거됨 | feature/jaehyun에서 온디바이스 한정 재통합 | ✅ |
| OBB 바운딩박스 라우팅 | 없음 | Android 온디바이스 OBB 경로 + 후처리 추가 | ⚠️ 검증 필요 |
| 찾기+확인 모드 | 별개 모드 | 병합, 각각 병렬 처리 | ✅ |
| 장애물 없음 TTS | 음성·텍스트 타이밍 불일치 | 싱크 수정 | ✅ |

---

## 수정 전 / 수정 후 — 서버·빌드

| 항목 | 수정 전 | 수정 후 | 상태 |
|------|---------|---------|------|
| requirements 파일 | 3개 (requirements.txt, requirements-server.txt, requirements-docker.txt) | 1개로 통합 | ✅ |
| start.bat / stop.bat | 있음 | 제거 (GCP Cloud Run 기준으로 전환) | 🗑️ |
| Docker Cloud Run 빌드 | 실패 | 로컬 전용 패키지 제외하여 수정 | ✅ |

---

## OBB 미검증 항목 (2026-05-04 기준)

> 문서: `docs/08_OBB_FPS_ANALYSIS.md` 참조 (현재 legacy/docs_archive/에 보관)

| 항목 | 상태 |
|------|------|
| 앱 assets에 실제 OBB ONNX 모델 포함 여부 | ⚠️ 미확인 (현재 yolo11n.onnx만 있을 가능성 높음) |
| Android 후처리 코드가 OBB 출력 구조와 일치 여부 | ⚠️ 미확인 |
| PreviewView와 분석 Bitmap 좌표계 일치 여부 | ⚠️ 미확인 |
| OBB 중복 제거 (현재 AABB 기준) | ⚠️ polygon IoU 미적용 |

확인 방법: Android Studio Logcat에서 `VG_PERF` 태그로 `modelName`, `obb flag`, `output shape` 확인.

---

## 주요 커밋

| 커밋 메시지 |
|------------|
| feat(android): TTS-text sync, FPS 50ms interval, SentenceBuilder comma pause |
| feature/myunggwang: 물건확인 모드 추가 |
| feature/myunggwang: 찾기+확인 모드로 병합, 두가지 모드 각각 병렬 처리 |
| Merge feature/jaehyun: 온디바이스 계단 탐지 통합 |
| Merge feature/myunggwang: 물건확인 모드 추가, 장애물 없음 TTS 싱크 수정 |
| Enable on-device OBB routing |
| fix(docker): Cloud Run 빌드 실패 수정 — 로컬 전용 패키지 제외 |
| chore: requirements 파일 3개 → 1개로 통합 |
| chore: start.bat, stop.bat 제거 |
