# 2026-05-03 변경 이력

> 범례: ✅ 반영 완료 | ❌ 반영 실패/롤백 | ⚠️ 부분반영/미검증 | 🗑️ 제거

## 핵심 이벤트

**MVP 스코프 확정 + 대규모 기능 제거.** OCR/SOS/지폐인식 등 16개 기능 legacy 이동.  
문장 중복 제거, CAUTION_CLASSES 추가, Qwen3-TTS fallback 추가.  
**INTERVAL_MS 700ms 시도 → FPS 1~2fps 하락 → 100ms로 즉시 복구.**

---

## 수정 전 / 수정 후 — 탐지 (detect.py)

| 항목 | 수정 전 | 수정 후 | 상태 |
|------|---------|---------|------|
| 탐지 클래스 | 80개 | bus 추가 → 81개 | ✅ |

---

## 수정 전 / 수정 후 — 문장 (sentence.py / SentenceBuilder.kt)

| 항목 | 수정 전 | 수정 후 | 상태 |
|------|---------|---------|------|
| 방향 표현 12시 | "바로" | "바로 앞" | ✅ |
| "앞" 중복 | "왼쪽 앞 약 3미터 앞에 의자가 있어요." | "왼쪽 앞 약 3미터에 의자가 있어요." | ✅ |
| 코앞 중복 | "바로 앞 코앞에 의자가 있어요." | "바로 코앞에 의자가 있어요." | ✅ |
| critical 차량 문장 | "위험! 바로 자동차! 조심!" | "위험! 바로 앞 약 3미터에 자동차가 있어요! 멈추세요!" | ✅ |
| 생활 물체 액션 | "왼쪽 앞 2미터에 키보드가 있어요. 오른쪽으로 피하세요." | "왼쪽 앞 약 2미터에 키보드가 있어요." (액션 제거) | ✅ |
| CAUTION_CLASSES | 없음 | 칼·가위·유리잔 등 주의 물체 별도 분류, 회피 액션 포함 | ✅ |

---

## 수정 전 / 수정 후 — Android

| 항목 | 수정 전 | 수정 후 | 상태 |
|------|---------|---------|------|
| INTERVAL_MS | 100ms | 700ms로 변경 (발열 고려) | ❌ FPS 1~2fps 하락 → 100ms로 즉시 복구 |
| FPS 재시작 시 rebind | 재시작마다 카메라 전체 rebind → FPS 튀김 | isCameraReady 플래그로 rebind 방지 | ✅ |
| 첫 프레임 지연 | lastStreamFrameTime 미초기화 → 700ms 지연 | 0L 초기화 | ✅ |
| 중지 후 STT | autoListenEnabled = false → 음성 안 들림 | true 유지 → "다시 시작" 음성으로 재개 가능 | ✅ |
| critical 재발화 주기 | 동일 문장 5초마다 반복 | 8초로 연장 | ✅ |
| 시작 안내 | "음성 안내를 시작할까요?" | "보이스가이드예요. 찾기, 확인, 질문 모드..." | ✅ |
| OCR / 바코드 | 활성 (captureForOcr, captureForBarcode) | legacy 이동, ML Kit 의존성 삭제 | 🗑️ |
| 빌드 에러 4개 | requestSmsPermission, triggerSOS, lastGpsSentTime, startGpsTracking 참조 | 전부 제거 | ✅ |

---

## 수정 전 / 수정 후 — UI

| 항목 | 수정 전 | 수정 후 | 상태 |
|------|---------|---------|------|
| 앱 아이콘 배경 | 파란 그라데이션 | 검정 #000000 | ✅ |
| 앱 테마 | Light.DarkActionBar (시작 시 흰 배경 플래시) | Theme.VoiceGuide (검정 스플래시) | ✅ |

---

## 수정 전 / 수정 후 — 서버

| 항목 | 수정 전 | 수정 후 | 상태 |
|------|---------|---------|------|
| TTS 백엔드 | Azure 단일 (키 없으면 무음) | Qwen3-TTS(HF API) → Azure → gTTS 3단계 fallback | ✅ |
| Azure import | 최상단 import (없으면 서버 크래시) | lazy import (함수 내부 try/except) | ✅ |
| easyocr | requirements-docker 포함 | 제거 (OCR 기능 legacy 이동) | 🗑️ |

---

## 반영 실패 상세

**INTERVAL_MS 700ms 롤백**
- 시도: 발열·배터리 절감을 위해 700ms로 변경
- 실패 이유: FPS 1~2fps대로 하락 (700ms 간격 = 이론 최대 1.43fps)
- 조치: 100ms로 즉시 복구

---

## 제거된 기능 16개 (legacy 이동)

| 기능 | 제거 이유 |
|------|----------|
| OCR (글자 읽어줘) | MVP 스코프 초과 |
| 바코드 인식 | MVP 스코프 초과 |
| 색상 분석 | MVP 스코프 초과 |
| 밝기 감지 | MVP 스코프 초과 |
| 식사 도우미 | MVP 스코프 초과 |
| 옷 매칭 / 옷 패턴 분석 | MVP 스코프 초과 |
| 지폐 인식 | MVP 스코프 초과 |
| 위치 저장 / 위치 목록 | MVP 스코프 초과 |
| 하차 알림 (GPS) | MVP 스코프 초과 |
| 약 복용 알림 | MVP 스코프 초과 |
| 다시 읽기 | MVP 스코프 초과 |
| 볼륨 조절 | MVP 스코프 초과 |
| 일시정지 / 재시작 | MVP 스코프 초과 |
| 긴급 SOS | MVP 스코프 초과 |
| 버스 번호 인식 | Vision/ML 검증 정확도 낮음 |
| 울퉁불퉁한 길 감지 | MVP 스코프 초과 |

---

## 테스트 결과

| 항목 | 결과 |
|------|------|
| pytest 28개 | 전부 통과 ✅ |
| Android 빌드 에러 | 0개 ✅ |
| Cloud Run | 코드 수정 완료, 재배포 필요 ⚠️ |

---

## 주요 커밋

| 커밋 메시지 |
|------------|
| fix: FPS 안정화·음성명령 개선·Qwen3-TTS 추가 |
| fix: Android 빌드 에러 4개 수정 (삭제된 함수 참조 제거) |
| fix: Cloud Run 빌드 실패 2가지 수정 |
| fix: merge 후 덮인 수정사항 복구 |
| fix: 테스트 실패 2건 수정 + 파일 정리 |
| refactor: OCR/바코드 비활성화 + 한국어 문장 자연스럽게 수정 |
| feature/myunggwang: 안쓰는 기능 legacy로 이동 |
| feature/myunggwang: 핵심MVP 3가지 명령과 GPS수신 외의 기능들 legacy로 옮김 |
| ui: Phase 7 프론트엔드 개선 (발열·아이콘·테마) |
