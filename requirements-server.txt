# GCP Cloud Run 배포용 — 핵심 기능만 포함 (빌드 시간 최소화)

# 딥러닝 (CPU 버전)
torch==2.4.1
torchvision==0.19.1
numpy==1.26.4

# 객체 탐지
ultralytics==8.3.2

# 이미지 처리
opencv-python-headless==4.10.0.84
Pillow>=10.0.0

# API 서버
fastapi==0.115.5
uvicorn==0.32.1
python-multipart==0.0.12
python-dotenv>=1.0.0

# TTS
gTTS==2.5.3
requests>=2.32.0
elevenlabs>=2.0.0

# DB (없으면 SQLite 자동 사용)
psycopg[binary]>=3.1.0
psycopg-pool>=3.1.0
