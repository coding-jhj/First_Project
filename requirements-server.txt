# GCP Cloud Run 배포용 — 서버 동작에 필요한 것만 포함
# Gradio, pygame, pyaudio 제외 → 이미지 크기 절감

# 딥러닝 (CPU 버전 — Cloud Run은 GPU 없음)
torch==2.4.1
torchvision==0.19.1
numpy==1.26.4

# 객체 탐지
ultralytics==8.3.2

# 이미지 처리
opencv-python-headless==4.10.0.84
Pillow>=10.0.0

# API 서버
fastapi==0.115.5
uvicorn==0.32.1
python-multipart==0.0.12
python-dotenv>=1.0.0

# TTS (gTTS만 — ElevenLabs는 환경변수로 선택)
gTTS==2.5.3
requests>=2.32.0
elevenlabs>=2.0.0

# OCR
easyocr>=1.7.0

# DB
psycopg[binary]>=3.1.0
psycopg-pool>=3.1.0
