# Python 3.10 기준 VoiceGuide 프로젝트
# 모든 버전은 상호 호환성 검증 완료
# (GCP Cloud Run 포함 단일 파일 — requirements-server.txt 통합)

# 딥러닝 프레임워크
# torch는 반드시 numpy보다 먼저 설치
# GPU 있으면: pip install torch==2.4.1+cu118 torchvision==0.19.1+cu118 --index-url https://download.pytorch.org/whl/cu118
# (dev-only) torch==2.4.1
# (dev-only) torchvision==0.19.1

# 수치 계산 - 버전 고정 필수!
# 2.x로 올리면 opencv, ultralytics 동작 충돌 발생
numpy==1.26.4

# 객체 탐지 모델 준비/학습 도구 (Android ONNX 모델 생성용)
# (dev-only) ultralytics==8.4.33

# 이미지 처리
# headless: GUI 없는 서버/도커 환경용
opencv-python-headless==4.10.0.84
Pillow>=10.0.0

# HTTP 요청 유틸리티 (서버 연결 점검 도구/통합 테스트)
requests>=2.32.0

# API 서버
fastapi==0.115.5
uvicorn==0.32.1
python-multipart==0.0.12

# 환경변수 로딩 (.env 파일)
python-dotenv>=1.0.0

# ONNX 내보내기 (tools/export_onnx.py용)
onnx
onnxscript

# 파인튜닝 데이터 수집 (train/prepare_dataset.py용)
ddgs

# Supabase / PostgreSQL 연동 (선택 사항, 기본은 SQLite)
psycopg[binary]>=3.1.0
psycopg-pool>=3.1.0

# 테스트
httpx
pytest
